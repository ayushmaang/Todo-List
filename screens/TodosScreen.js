/* eslint-disable */
import React from "react";

import TodosContainer from "../components/TodosContainer";

// wrapper component for TodosContainer
const TodosScreen = (props) => <TodosContainer {...props} />;

export default TodosScreen;
